import styled from "styled-components";

export const TreeContainer = styled.div`
  position: relative;
  width: 100%;
  overflow-x: auto;
  min-width: 512px;
`;

const S = { TreeContainer };

export default S;
